<nav>
  <div class="nav-wrapper">
    <%= link_to 'MANNA', home_path, class: "brand-logo left" %>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <li><%= link_to(home_path) do %> ACAC Pittsburgh PA<% end %></li>
        <li><%= link_to 'Home', home_path %></li>
        <li><%= link_to 'Submit', new_case_path %></li>
        <li><%= link_to 'Cases', cases_path %></li>
        <li>
            <% if logged_in? %>
              <!-- <%= current_user.proper_name %> | <%= current_user.role %><br> -->
              <%= link_to 'Logout', logout_path %>
            <% else %>
              <%= link_to 'Login', login_path %>
            <% end %>
        </li>
        <li>
            <%= form_tag(cases_path, :method => "get", id: "search-form") do %>
              <div class = "row">
                <div class = "col s12"><%= text_field_tag :search, params[:search], placeholder: "search cases" %></div>
              </div>

            <% end %>
          </li>
      </ul>
  </div>
</nav>