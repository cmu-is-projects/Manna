   <br>
   <br>
   <% if !@active_users.empty? %>
   <h1>Deacons and Staff</h1>
  <div class="row">
      <% @active_users.each do |user| %>
      <%= render partial: "user_card", locals: {user: user}%>
<%end%>
</div>

  <% else %>
  <p>Nothing to show.</p>
  <% end %>



<i class="fa fa-pencil" aria-hidden="true"></i>