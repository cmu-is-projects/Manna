<!DOCTYPE html>
<html>
<head>
  <title>Manna</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">


  <!-- add favicon later -->
  <%= favicon_link_tag 'favicon.png' %>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>


</head>
<body>
 <%= render 'partials/navigation' %>
     <!-- search bar -->
     
    <%= form_tag(cases_path, :method => "get", id: "search-form") do %>
      <div class = "row">
        <div class = "col s8"><%= text_field_tag :search, params[:search], placeholder: "Search Cases" %></div>
        <div class = "col s2 offset-s2 waves-effect waves-light btn black-text white"><%= submit_tag "Search", :name => nil,   :id => 'search_button' %></div>
      </div>

    <% end %>
  
  <div class = "container">

    <%= yield %>

  </div>

  <%= render 'partials/footer' %>
</body>
</html>
