<%= render partial: "partials/case_search"%>

<% if logged_in? %>
<!-- <p id="notice"><%= notice %></p> -->

<!-- <div class="card"> -->
  <% if !@cases.empty? %>
  <h2>Listing Cases</h2>
  <div class="center">
     <%= link_to '+ NEW CASE', new_case_path, class: "btn orange lighten-2" %>
     </div>
     <br>
  <!-- <ul class="collection"> -->

    <%# for c in @cases %>
      <%#= render partial: "partials/case_table_row", locals: {c: c, vote: @cases_need_voting} %>

    <%= render partial: "partials/timeline", locals: {all_cases: @cases}%>

     <%#end%>

   <!-- </ul> -->
   <% else %>
   <p>No cases in the system.</p>
   <% end %>

   <% else %>
   <p>please log in</p>

   <% end %>
 <!-- </div> -->

</li>
</ul>
