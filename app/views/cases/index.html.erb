<%= render partial: "partials/case_search"%>

<% if logged_in? %>
<!-- <p id="notice"><%= notice %></p> -->

<!-- <div class="card"> -->
<h2>Cases</h2>
<div class="center">
 <%= link_to '+ NEW CASE', new_case_path, class: "btn orange lighten-2" %>
</div>
<br>
<% if !@cases.empty? %>
<%= render partial: "partials/timeline", locals: {all_cases: @cases}%>

<% else %>
<h5>There are currently no cases in the system.</h5>
<% end %>

<% else %>
<p>Please log in</p>
<% end %>


</li>
</ul>
